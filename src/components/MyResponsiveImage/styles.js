import { StyleSheet } from "react-native";

const styles = StyleSheet.create({  
  responsiveImage: {
    position: 'absolute',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
  },

});

export default styles;